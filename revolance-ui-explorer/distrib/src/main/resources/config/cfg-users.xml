<?xml version="1.0"?>
<configuration>

    <users>

        <user>
            <id>googler</id>
            <appId>google</appId>
            <home>http://www.google.fr</home>
            <browserType>Firefox</browserType>
        </user>

        <!-- A user profile to be used to explore the appplication {user.appId}
        <user>
            <id>user_A</id> the user.id shown in the logs / used as folder name for the content reporting
            <appId>website</appId> define on which app this user should be connected
            <login>user_A</login> (optional)
            <password>pwd</password> (optional)
            <newPassword>newPwd</newPassword> (optional)
            <home> </home>
            <browserType>Firefox</browserType> (Chrome or Firefox)
        </user>
        -->

    </users>

</configuration>